{
  "name": "pi-extension-codelin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-equivalent code search: symbols, files, and call paths from a local code index",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "node --test 'test/*.test.ts'"
  },
  "dependencies": {
    "@colbymchenry/codegraph": "1.5.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
